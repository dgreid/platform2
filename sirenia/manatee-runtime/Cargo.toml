[package]
name = "manatee_runtime"
version = "0.1.0"
description = "API endpoint for TEEs to communicate with Trichechus."
authors = ["The Chromium OS Authors"]
edition = "2018"

[dependencies]
libsirenia = { path = "../libsirenia" } # provided by ebuild
serde = { version = "1.0.114", features = ["derive"] }
